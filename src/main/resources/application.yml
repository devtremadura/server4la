---
spring:
  config:
    activate:
      on-profile: demo
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    virtual-host: /

  data:
    redis:
      host: localhost
      port: 6379
      password: test123
      database: 0
      timeout: 30000
      pool:
        maxIdle: 8
        minIdle: 0
        maxActive: -1
        maxWait: 30000
      repositories:
        enabled: true

#----------------------------------------

